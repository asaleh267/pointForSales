import { createStyles, Theme } from "@material-ui/core/styles";

export default (theme: Theme) => {
  return createStyles({
    productContainer: {
        width: 45,
        height: 45,
        padding: 8
    }
  });
}
